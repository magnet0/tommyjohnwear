
                <!-- m-product-color -->
                <fieldset class="product-color collapsed">
                    <legend>Color:</legend>
                    {{# product.colors }}
                    	<input type="radio" id="color-{{ id }}" name="color" value="{{ id }}"><label class="product-color-label" for="color-{{ id }}" title="{{ name }}" style="background-color:#{{ hex }}">{{ name }}</label>
                    {{/ product.colors }}
                </fieldset>
                <!-- /m-product-color -->