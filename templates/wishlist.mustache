{{> head }}
		
<article role="main">
    <div class="content row">
        <div class="three centered column">
            <h1>{{ meta.title }}</h1>
            <ul class="wishlist no-bullet">
                {{# wishlist }}
                {{> m-account-wishlist-item }}
                {{/ wishlist }}
            </ul>
        <a href="">Email this list to someone</a>
        </div>
	</div>
</article>
		
{{> sidebar }}

{{> foot }}