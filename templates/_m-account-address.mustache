<div class="vcard">
    <div class="fn org">{{ name }}</div>
    <div class="adr">
        <span class="street-address">{{ address }}</span>, 
        <span class="extended-address">{{ address2 }}</span>, 
        <span class="locality">{{ city }}</span>, <span class="region">{{ state }}</span> <span class="postal-code">{{ zip }}</span>
    </div>
</div>