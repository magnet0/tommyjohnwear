
        <!-- m-image-viewer -->
        <div class="m-product-image-viewer">
            <div class="image-viewer-stage tab-content">
                {{# product.images }}
                <div id="{{ name }}" class="image-viewer-display tab-pane{{# active }} active{{/ active }}" style="background-image:url({{ large }})">
                </div>
                {{/ product.images }}
            </div>
            <ul class="image-viewer-nav tabs">
                <li class="image-viewer-nav-top"><i class="icon-eye-open"></i></li>
                {{# product.images }}
                <li class="image-viewer-thumb{{# active }} active{{/ active }}">
                    <a href="#{{ name }}" data-toggle="tab"><img src="{{ thumb }}" alt=""></a>
                </li>
                {{/ product.images }}
            </ul>
        </div>
        <!-- /m-image-viewer -->